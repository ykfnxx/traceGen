# traceGen 开发约定

## 项目方向

traceGen 用于合成 LLM serving 接收的请求流，重点表达多轮时序、上下文增长和前缀复用。

当前重构设计见 [docs/config-driven-refactor.md](docs/config-driven-refactor.md)。阅读后再开展相关工作；设计方案不代表功能已经实现，运行接口以代码和 README 为准。

重构遵循以下原则：

- 运行时根据配置生成负载，不再依赖 Weka、LMSYS 等数据集的 session 模板采样。
- 论文提供规律和参考配置参数，不引入自动校准、迭代拟合或追踪论文曲线的优化器。
- 区分 task profile、client、session 和 request。任务配置描述行为族，client 是持续流量来源，session 承载多轮状态。
- 整体配置控制负载组成、流量强度、burst 和波峰波谷；具体参数口径以设计文档和当次任务约定为准。
- 前缀身份可以合成，不要求真实文本或生产 block hash，但共享、继承和分叉关系必须一致。
- 图表展示配置产生的结果，明确区分可编辑的输入曲线和统计输出曲线。

## 语义约束

- 明确区分 `sessions/s` 与 `requests/s`，以及 client 数量占比、session 占比和请求占比。
- 明确区分人类轮次与 LLM 请求序号；一个人类轮次可以包含多次 Agent 请求。
- 请求到达间隔与工具/用户等待时间不是同一个量，不混用或重复累加响应耗时。
- 输入长度应由前缀、历史和新增内容推导；前轮模型输出不能被重复计入。
- 同样的长度不意味着内容相同。合成身份必须体现前缀依赖和配置允许的共享范围。
- 可复用前缀比例不等于有限容量缓存命中率；实际缓存读取 token 数不等于内容公共前缀长度。
- burst 对新 session 发起的影响与对已有 session 续发的影响必须明确，不隐式压缩轮间时间。
- 论文报告的统计、为预设补充的假设和人工示例参数分别注明，不把示意数据称为生产实测结果。

## 工作方式

- 沟通和设计文档默认使用中文，沿用代码已有命名风格。
- 开始修改前检查分支、HEAD 和工作区，保留已有修改。
- 用户要求讨论或设计时，只分析或更新指定文档；实现任务按授权范围推进。
- 优先最小、清晰的实现，避免提前引入复杂学习模型或大量可选机制。
- 只维护 version 3 配置驱动生成路径；不要重新引入数据集采样、自动校准或兼容分支。新增界面必须复用 Python 生成核心。
- 改变配置协议时，同步更新示例与相关文档。
- 设计文档直接维护最终方案：修改对应章节，不追加讨论记录、旧方案或修订过程。
- 保持随机生成可复现。调整图表窗口、平滑和筛选不能改变底层 trace。
- 不自动提交或推送，除非用户要求。

## 验证

从仓库根目录运行现有测试：

```bash
python3 -m unittest discover -s tests -q
```

按改动范围选择验证。生成逻辑重点检查时间顺序、前缀继承与隔离、长度核算、随机复现和窗口边界；不要把通过单元测试等同于符合真实生产分布。

纯文档修改检查相对链接、示例语义和 `git diff --check`，无需运行整套生成测试。

## 文档入口

- [重构设计](docs/config-driven-refactor.md)：配置驱动的 client pool、多轮合成和交互曲线。
- [中文 README](README_ZH.md) / [English README](README.md)：当前已实现功能与运行方法。
- [配置协议](docs/configuration.md)：支持的字段和数值语义。
- [工作台说明](docs/workbench.md)：交互编辑、对比和导出。

如文档与当前代码不一致，说明差异并按当次任务范围处理，不静默扩大改动。
